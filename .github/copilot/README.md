# GitHub Copilot Configuration

This directory contains configuration settings for GitHub Copilot that specify:

- The main code directory is `app/`
- Package manager is `pnpm`
- TypeScript requires separate type imports
- Unit tests should be run with `pnpm test`

## Configuration Files

- `options.json`: Contains Copilot-specific settings for this workspace
